import type { Request, Response } from "express";
/**
 * Controller for marking notifications as read.
 * Decides which service to call based on body params.
 */
export declare function markReadController(req: Request, res: Response): Promise<Response<any, Record<string, any>>>;
//# sourceMappingURL=pushNotifcationController.d.ts.map