//
//  BuildConfig.xcconfig
//  App
//
//  Shared build variables that let Xcode invoke the web build from
//  shift-scan-app/client before compiling the native target.

// Root of the web client relative to ios/App.
CLIENT_DIR = $(PROJECT_DIR)/../client

// Node tooling used inside Run Script phases.
CLIENT_NODE_BIN = /usr/bin/env node
CLIENT_NPX_BIN = /usr/bin/env npx

// Commands that build and sync the Capacitor web assets.
CLIENT_BUILD_COMMAND = npm run static
CLIENT_SYNC_COMMAND = npx cap copy ios

// Toggle to skip npm install when CI already prepared node_modules.
SKIP_CLIENT_INSTALL = NO

// Script snippet (for reference) that can be dropped into a Run Script phase:
// if [ "$(SKIP_CLIENT_INSTALL)" != "YES" ]; then npm install; fi
// npm run static
// npx cap copy ios
